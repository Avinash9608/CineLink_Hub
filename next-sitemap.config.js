/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://cine-link-hub.vercel.app', // replace with your actual site URL
    generateRobotsTxt: true, // (optional) generate robots.txt
    sitemapSize: 5000,
  };
  